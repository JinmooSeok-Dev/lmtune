# gaie values for wide-EP — minimal EPP
inferencePool:
  modelServers:
    matchLabels:
      llm-d.ai/inferenceServing: "true"

epp:
  service:
    type: ClusterIP
  resources:
    requests:
      cpu: "200m"
      memory: 512Mi
    limits:
      cpu: "1"
      memory: 1Gi
